#include <bits/stdc++.h>
#include "Geometry - Vectors.cpp"
using namespace std;
template <typename T>
struct segment2D {
    vec2D<T> p1, p2;
    double distance_to(const vec2D<T>& p) {

    }
    bool contains(const vec2D<T>& p) {
        
    }
};
template <typename T>
bool point_on_segment() {

}
